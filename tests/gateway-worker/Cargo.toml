[workspace]
[package]
name = "application-gateway-worker-fixture"
version = "0.0.0"
edition = "2021"
publish = false
[lib]
crate-type = ["cdylib"]
[dependencies]
distributed = { path = "../..", default-features = false, features = ["gateway-worker"] }
worker = "=0.8.5"
serde_json = "1"
[package.metadata.wasm-pack.profile.release]
wasm-opt = false
