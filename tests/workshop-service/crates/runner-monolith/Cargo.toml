[package]
name = "workshop-runner-monolith"
version.workspace = true
edition.workspace = true
publish = false
description = "Thin runner: one process, all handlers (catalog + orders)"

[[bin]]
name = "workshop-monolith"
path = "src/main.rs"

[dependencies]
distributed = { workspace = true }
workshop-service = { path = "../service" }
workshop-readmodels = { path = "../readmodels" }
tokio = { workspace = true }
axum = { workspace = true }
serde_json = { workspace = true }
