[package]
name = "e2e-readmodels"
version.workspace = true
edition.workspace = true
publish = false
description = "Todo read models (TodoView) + project manifest"

[dependencies]
distributed = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
todo-domain = { path = "../todo-domain" }
chat-domain = { path = "../chat-domain" }
blob-domain = { path = "../blob-domain" }
