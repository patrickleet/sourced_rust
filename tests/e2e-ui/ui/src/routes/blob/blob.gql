# Blob games — joins `owner` from imported auth_users (Zitadel ingestor).
query BlobGames {
	blob_games {
		game_id
		owner_id
		score
		player_dead
		current_level
		current_level_completed
		map_json
		status
		owner {
			user_id
			display_name
			email
			status
		}
	}
}
