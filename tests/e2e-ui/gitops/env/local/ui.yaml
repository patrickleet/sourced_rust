apiVersion: hops.local/v1alpha1
kind: Application
metadata:
  name: e2e-ui-ui
spec:
  source:
    path: ../../../ui/.gitops/deploy
    helm:
      values:
        local: true
        appRuntime: cluster-dev
  destination:
    namespace: hops-wt-default
  syncPolicy:
    prune: false
