# PSQL platform (local)

| File | Kind |
|------|------|
| `stack.yaml` | `PSQLStack` XR — CNPG + StorageClass `psql` on `local-path` |

AuthStack embedded DB and future e2e-ui `PSQLCluster` claims use `spec.storage.class: psql`.
