[package]
name = "application-runtime"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
application-contract-only = { path = "../application-contract-only", features = ["application-runtime"] }
