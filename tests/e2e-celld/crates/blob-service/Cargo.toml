[package]
name = "e2e-celld-blob"
version.workspace = true
edition.workspace = true
publish = false
description = "Blob Atomic command service crate (in-process; not a cell)"

[dependencies]
distributed = { workspace = true }
blob-domain = { path = "../../../e2e-ui/crates/blob-domain" }
